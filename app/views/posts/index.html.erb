<% @posts.each do |post| %>
	<div class ="post_wrapper">
		<h2 class "title" ><%= link_to post.title, post%></h2>
		
		<p class="author">Submitted <%= time_ago_in_words(post.created_at) %> by <%= post.user.name %></p>﻿
  
	<img class="avatar"><%= image_tag(post.user.avatar.url(:small))  %></img>
	 <div class="btn-group">
 
      <%= link_to like_post_path(post), method: :put, class: "btn btn-default btn-sm" do %>
        <span class="glyphicon glyphicon-chevron-up"></span>
        Upvote
        <%= post.get_upvotes.size %>
      <% end %>
      <%= link_to dislike_post_path(post), method: :put, class: "btn btn-default btn-sm" do %>
        <span class="glyphicon glyphicon-chevron-down">
        Downvote
        <%= post.get_downvotes.size %>
      <% end %>
    </div>
  <div calss="saveme">
  
  </div>
  </div>

<% end %>
