<%= form_for Post.new do |f| %>
	
	
  <p>	
  <%= f.label(t("posts.title")) %><br>
  <%= f.text_field :title %>
  </p>

  <p>
  <%= f.label(t("posts.body")) %><br>
  <%= f.text_area :body %>
  </p>

  <p>
  <%= f.submit(t("posts.create_post")) %>
  </p>
<% end %>
